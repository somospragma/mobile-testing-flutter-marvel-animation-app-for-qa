name: marvel_animation_app
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_riverpod: ^2.4.10
  flutter_screenutil: ^5.9.3
  go_router: ^13.1.0

  cupertino_icons: ^1.0.2
  mocktail: ^1.0.4
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env